# Sample environment variables - COPY this file to .env and fill in your own values
# DO NOT commit the actual .env file with real credentials

# Server configuration
PORT=5000
NODE_ENV=development

# MongoDB database connection
MONGODB_URI=mongodb://localhost:27017/yourdbname

# Firebase/Google Cloud credentials
# These should be copied from your serviceAccountKey.json

# Firebase project info
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=your-service-account-email@your-project-id.iam.gserviceaccount.com
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com

# API keys (if applicable)
OPENAI_API_KEY=your-openai-api-key

# Other configuration
DEFAULT_USER_CREDITS=1000
ADMIN_USER_EMAIL=admin@example.com