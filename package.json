{
    "name": "8020-best",
    "version": "1.0.0",
    "description": "8020.best - AI-Powered Task Prioritization (Vercel deployment)",
    "scripts": {
        "frontend": "cd frontend && npm run dev",
        "backend": "cd backend && npm run dev",
        "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "replicate": "^0.34.1"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}