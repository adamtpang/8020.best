import React from 'react';

// Temporarily simplified ProtectedRoute that just renders children
const ProtectedRoute = ({ children }) => {
    // Always render children without any protection for now
    return children;
};

export default ProtectedRoute;