import React from 'react';

// Temporarily simplified AdminRoute that just renders children
const AdminRoute = ({ children }) => {
    // Always render children without any protection for now
    return children;
};

export default AdminRoute;